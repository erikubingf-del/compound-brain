# [PROJECT_NAME] — Codex Adapter

This repo uses a single canonical control plane.

Read before project-specific work:
- `CLAUDE.md`
- `.brain/memory/project_context.md`
- `.brain/memory/feedback_rules.md`

If `.claude/` exists, also read:
- `.claude/settings.local.json`
- `.claude/departments/*.md`
- `.brain/state/approval-state.json`

Do not create a parallel repo memory or repo-local runtime outside `.brain/`
and `.claude/`.

